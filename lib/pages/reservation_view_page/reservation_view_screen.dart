import 'package:flutter/material.dart';

class ReservationViewScreen extends StatelessWidget {
  const ReservationViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
